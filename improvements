You don't need "metadata.json" anymore. The original file name is enough.

Files generated by vocal_split.py should be named "lead.wav" and "backing.wav".
lead and backing should appear below "vocals.wav" if it exists.
lead and backing should start disabled on the UI.